"use client";
import { useEffect,useState } from "react";
import "../../globals.css";import "../admin.css";import "./menu-admin.css";
type Item={id:string;name:string;description:string;price_cents:number;category:string;image:string;available:boolean;sort_order:number};
export default function MenuAdmin(){const[items,setItems]=useState<Item[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetch("/api/admin/menu").then(async r=>{if(r.status===401)throw Error("unauthorized");const d=await r.json();setItems(d.items||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);async function toggle(item:Item){const r=await fetch(`/api/menu/${item.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({available:!item.available})});if(r.ok)setItems(x=>x.map(i=>i.id===item.id?{...i,available:!i.available}:i));}return <main className="menu-admin shell"><header className="admin-nav"><a className="brand" href="/admin"><span>Z</span> Zee&apos;s Kitchen</a><div><a href="/admin">Orders →</a><a href="/">Storefront →</a></div></header><div className="admin-heading"><div><p className="eyebrow">MENU MANAGEMENT</p><h1>Your menu.</h1><p>Control what customers can order.</p></div></div>{error?<div className="admin-empty">{error==="unauthorized"?<>Sign in required. <a href="/admin/login">Open login →</a></>:"Unable to load menu."}</div>:loading?<div className="admin-empty">Loading menu…</div>:<section className="menu-manager"><div className="manager-head"><strong>{items.length} menu items</strong><button className="primary" type="button">Add item <span>+</span></button></div>{items.map(item=><article className="manager-item" key={item.id}><div className="manager-image" style={{backgroundImage:`url(${item.image})`}}/><div className="manager-info"><strong>{item.name}</strong><span>{item.category}</span><small>{item.description}</small></div><strong>${(item.price_cents/100).toFixed(2)}</strong><button className={`availability ${item.available?"on":"off"}`} onClick={()=>toggle(item)}>{item.available?"Available":"Sold out"}</button></article>)}</section>}</main>}
