import {NextResponse} from "next/server";import {cookies} from "next/headers";import {adminCookieName,isAdminToken} from "@/lib/admin-auth";import {sendOneSignalPush} from "@/lib/onesignal";
export async function POST(){if(!isAdminToken((await cookies()).get(adminCookieName())?.value))return NextResponse.json({error:"Unauthorized"},{status:401});const result=await sendOneSignalPush({title:"Zee's Kitchen test alert 🔔",message:"Push notifications are connected. Your order and booking alerts will arrive here.",url:"/admin"});if(!result.sent)return NextResponse.json({error:"Push notification could not be sent",reason:result.reason},{status:502});return NextResponse.json({ok:true,id:result.id})}
